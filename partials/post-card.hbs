{{!-- partials/post-card.hbs --}}
{{!-- Reusable post card: used in index.hbs, tag.hbs, author.hbs, search.hbs --}}

<article class="{{post_class}} glass-card group animate-fade-in">
  <a href="{{url}}" class="block no-underline" aria-label="Read: {{title}}">

    {{!-- Feature Image --}}
    {{#if feature_image}}
    <div class="relative w-full h-44 overflow-hidden rounded-t-gx-lg">
      <img
        src="{{img_url feature_image size="m"}}"
        alt="{{title}}"
        class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
        loading="lazy"
      >
    </div>
    {{/if}}

    <div class="p-5 space-y-3">

      {{!-- Tag + Reading Time row --}}
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-2">
          {{#primary_tag}}
            <span class="mono-badge">{{name}}</span>
          {{/primary_tag}}
          {{#if featured}}
            <span class="mono-badge border-amber-500/40 text-amber-400">★ featured</span>
          {{/if}}
        </div>
        <span class="font-mono text-label text-gx-secondary">{{reading_time}}</span>
      </div>

      {{!-- Title --}}
      <h2 class="font-display font-semibold text-gx-primary text-body leading-snug
                 group-hover:text-gx-tertiary transition-colors duration-200 line-clamp-2">
        {{title}}
      </h2>

      {{!-- Excerpt --}}
      {{#if custom_excerpt}}
        <p class="text-gx-secondary text-sm leading-relaxed line-clamp-2">{{custom_excerpt}}</p>
      {{else}}
        <p class="text-gx-secondary text-sm leading-relaxed line-clamp-2">{{excerpt words="20"}}</p>
      {{/if}}

      {{!-- Meta: Author + Date --}}
      <div class="flex items-center justify-between pt-1 border-t border-gx-border/60">
        <div class="flex items-center gap-2">
          {{#primary_author}}
            {{#if profile_image}}
              <img src="{{img_url profile_image size="xs"}}"
                   alt="{{name}}"
                   class="w-5 h-5 rounded-full border border-gx-border object-cover">
            {{else}}
              <div class="w-5 h-5 rounded-full bg-gx-tertiary flex items-center justify-center flex-shrink-0">
                <span class="font-mono text-gx-on-primary text-xs leading-none">&gt;_</span>
              </div>
            {{/if}}
            <span class="font-mono text-label text-gx-secondary">{{name}}</span>
          {{/primary_author}}
        </div>
        <time class="font-mono text-label text-gx-secondary"
              datetime="{{date format="YYYY-MM-DD"}}">
          {{date format="MMM D, YYYY"}}
        </time>
      </div>

    </div>
  </a>
</article>
